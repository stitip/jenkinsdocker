pipeline {
  agent any

  stages {
    stage('Deploy App') {
      steps {
        script {
		  sh 'kubectl get pod'
          kubernetesDeploy(configs: "myweb.yaml", kubeconfigId: "mykubconfig")
        }
      }
    }

  }

}